# Treat ESPN automation as experimental and separate

An optional browser companion may later emit normalized draft events but will not collect credentials, cookies, or unrelated browsing data. ESPN page logic remains outside the primary draft engine and is disabled by default.
