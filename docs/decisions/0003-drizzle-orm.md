# Use Drizzle for typed infrastructure access

Drizzle owns schema definitions, explicit SQL migration generation, and typed repository implementations. Domain and UI code never call Drizzle directly.
